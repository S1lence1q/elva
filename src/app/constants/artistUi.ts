/** Hero pill — artist hub from search/Topic heuristics, not a verified catalog. */
export const ARTIST_PROFILE_BADGE = 'Artist profile';

/** Track list section heading on full artist view. */
export const ARTIST_TRACKS_SECTION_LABEL = 'Tracks';

/** Hint on searchable artist cards (queue + landing search). */
export const ARTIST_SEARCH_CARD_HINT = 'View discography';
